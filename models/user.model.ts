export const user = {
    id: Number,
    email: String,
    password: String,
    role: "Store Owner" || "Buyer" || "Warehouse Owner" || "Driver",
    date: String,
    active: Boolean,
    userName: String,
    phoneNumber: String,
}